{
  "name": "campuslink-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "sync-types": "cd backend && npx prisma generate",
    "install:all": "npm install",
    "build:all": "npm run build --workspaces --if-present",
    "dev": "vite --host"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.32.0",
    "bcrypt": "^6.0.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.0",
    "pinia": "^3.0.4",
    "pinia-plugin-persistedstate": "^4.7.1"
  }
}
